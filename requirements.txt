# Pinned to tested versions for reproducible deploys.
# A rebuild on Render installs exactly these — no surprise breakage from a new
# upstream release. Bump deliberately, then re-run the test suites.

# ── Dashboard (Compliance read-only view) ──
streamlit==1.57.0
pandas==2.3.1
plotly==6.7.0

# ── Shared data layer ──
supabase==2.30.0

# ── Payments (payments/ provider abstraction) ──
stripe==15.3.0             # Stripe provider (disabled by default; PAYMENT_PROVIDER=stripe re-enables)
httpx==0.28.1              # Checkout.com provider (REST) — also a supabase dependency

# ── PDF report generation (the paid deliverable) ──
reportlab==4.5.1

# ── Lab-report photo/PDF extraction (Claude vision) ──
anthropic==0.109.2
pillow==12.2.0            # image downscale before vision call (extract.py)

# ── HTTP API (for Vonage/n8n voice pipeline + Android app) ──
fastapi==0.136.3
uvicorn[standard]==0.34.0
PyJWT[crypto]==2.10.1      # Clerk JWT verification via JWKS

# ── Agent input (MCP server for Claude Desktop; not used by the web services) ──
mcp[cli]==1.27.2
