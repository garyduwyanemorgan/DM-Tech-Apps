# ── Dashboard (DECCA read-only view) ──
streamlit>=1.30.0
pandas>=2.0.0
plotly>=5.18.0

# ── Shared data layer ──
supabase>=2.0.0

# ── Agent input (MCP server for Claude Desktop) ──
mcp[cli]>=1.2.0

# ── HTTP API (for Vonage/n8n voice pipeline) ──
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
